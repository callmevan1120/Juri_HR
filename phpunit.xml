<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="log"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- Test-only gate flippers: in testing mode LicenseGuard only checks these are >=32 chars
             (never used cryptographically). Production uses ENTERPRISE_OBFUSCATOR_KEY / ENTERPRISE_ADDON_SALT_*
             read from the gitignored .env, so these dummy values unlock nothing real. -->
        <env name="TEST_ENTERPRISE_OBFUSCATOR_KEY" value="testing-only-obfuscator-gate-flag-not-a-secret"/>
        <env name="TEST_ENTERPRISE_ADDON_SALT_TOKO_POS" value="testing-only-toko-pos-addon-gate-flag-not-secret"/>
    </php>
</phpunit>
